[project]
name = "alpha-othello"
version = "0.1.0"
description = "Othello rules engine and self-play DQN agent"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "torch>=2.13.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
